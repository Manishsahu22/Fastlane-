default_platform(:ios)

platform :ios do
  desc "Build iOS application"
  lane :build do
    puts "======================================"
    puts "Starting iOS build..."
    puts "======================================"

    # Install CocoaPods dependencies
    cocoapods(
      clean_install: true,
      podfile: "./ios/Podfile" # <--- Add this line
    )
    # Automatically use the workspace generated by CocoaPods
   build_app(
      workspace: "ios/QliniqKiosk.xcworkspace", 
      scheme: "QliniqKiosk",
      configuration: "Release",
      
      # Change this to build for a Simulator
      destination: "platform=iOS Simulator,name=iPhone 15", 
      
      # Tell Fastlane NOT to try and package an .ipa file
      skip_archive: true, 
      
      skip_codesigning: true
    )

    puts "======================================"
    puts "Build completed!"
    puts "Output: build/qliniq-kiosk.ipa"
    puts "======================================"
  end
end
